onkeyup = function(e){
    e = e || event;
    var map = [];
    map[e.keyCode] = e.type == 'keyup';
    if(map[32]){
        if()
    }
}
onkeydown = function(e){
    e = e || event;
    var map = [];
    map[e.keyCode] = e.type == 'keydown';

}